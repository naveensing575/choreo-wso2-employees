schemaVersion: 1.1

# Define the endpoints for the service
endpoints:
  - name: employees-service
    displayName: Employees Service
    service:
      basePath: /employees
      port: 8080
    type: REST
    networkVisibilities:
      - Public
      - Organization
    schemaFilePath: openapi.yaml

# Runtime configurations
configurations:
  env:
    - name: PORT
      value: "8080"
